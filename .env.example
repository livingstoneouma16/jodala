# Local development configuration. Copy this file to .env and replace values
# marked with a placeholder before using shared services.
APP_ENV=development
SECRET_KEY=dev-secret-change-before-production
JWT_SECRET_KEY=dev-jwt-secret-change-before-production
DATABASE_URL=postgresql://postgres:YOUR_POSTGRES_PASSWORD@localhost:5432/jodala
RATELIMIT_STORAGE_URI=memory://
COOKIE_SECURE=false

# Payment gateway: M-Pesa Daraja is the default STK Push gateway. Leave the
# sandbox defaults in use for testing, then provide production credentials
# and a publicly reachable HTTPS callback URL before going live.
MPESA_ENVIRONMENT=sandbox
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=
MPESA_SHORTCODE=
MPESA_PASSKEY=
MPESA_CALLBACK_URL=

# Optional CloudPay STK Push gateway. Set PAYMENT_GATEWAY=cloudpay only after
# its credentials and public HTTPS callback URL have been configured.
PAYMENT_GATEWAY=mpesa
CLOUDPAY_ENVIRONMENT=sandbox
CLOUDPAY_API_KEY=
CLOUDPAY_API_SECRET=
CLOUDPAY_TILL_NUMBER=
CLOUDPAY_CALLBACK_URL=

# Passkey login for local development
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_NAME=Jodala Microfinance
WEBAUTHN_ORIGIN=http://localhost:5000

# Scheduled background jobs
ENABLE_OVERDUE_SCHEDULER=true
OVERDUE_REMINDER_HOUR=8
OVERDUE_REMINDER_MINUTE=0
ENABLE_BACKUP_SCHEDULER=false
SCHEDULER_TIMEZONE=UTC
BACKUP_DIR=backups
BACKUP_RETENTION=7
CAMPAIGN_MAX_RECIPIENTS=250

# TextSMS automatic customer notifications. Once enabled, repayment receipts,
# loan disbursements, and daily overdue reminders are sent automatically.
TEXTSMS_API_KEY=
TEXTSMS_PARTNER_ID=
TEXTSMS_SENDER_ID=
SMS_NOTIFICATIONS_ENABLED=false
# ChatGPT campaign drafting (optional)
OPENAI_API_KEY=
# Optional: defaults to gpt-5
OPENAI_CAMPAIGN_MODEL=gpt-5
